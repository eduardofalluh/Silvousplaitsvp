# Copy to .env for local Netlify dev (netlify dev).
# Current setup uses honeypot only – no env vars required.
# If you add reCAPTCHA later, set your secret in Netlify only (never commit real keys).
